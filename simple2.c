#include <stdio.h> 	     
int main(){  				 
	int a, b, c, d; // declare 3 variables a, b, and c
	a = 100;
	b = 200;
	c = a + b;   // do a simple arithmetic operation
	d = a * b;	
	printf("The value of %d + %d is %d\n", a, b, c);	
	printf("The value of %d * %d is %d\n", a, b, d);
	return 0;
}
